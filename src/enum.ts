export  enum EleType {
  /**
   * 加载
   */
  load = 0,
  /**
   * 保存
   */
  save = 1,
  /**
   * 左边患者病历点击
   */
  patientmedical = 2,
  /**
   * 重新打开病历文本
   */
  openDocument =3,
}
export  enum DocumentFormat {
  PlainText = 1,
  Rtf = 2,
  OpenXml = 4
}
